var w = 800, h = 600;
var score = 0;
var player, keyboard;
var diamond, star, diamond,diamonds, platform, scoreText, gameOverText, bestText,star,stars, platforms, timeText, btn, btnLeft, btnRight;
var game = new Phaser.Game(w, h, Phaser.CANVAS, '');

var basicGame = function(){
}